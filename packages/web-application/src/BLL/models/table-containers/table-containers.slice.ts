import { createSlice } from '@reduxjs/toolkit';

interface TableContainersState {}

const initialState = {};

const tableContainers = createSlice({
    name: 'table-containers',
    initialState,
    reducers: {},
});

export default tableContainers.reducer;
